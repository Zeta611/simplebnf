%% Default values for the `simplebnf' keys.
% default key-values
\keys_set:nn { simplebnf }
  {
    prod-delim = ;;,
    new-line-delim = \|,
    single-line-delim = //,
    comment = :,
    line-break-delim = \\,
    relation = {::=|->|:in:},
    relation-sym-map =
      {
        {::=} = {\ensuremath{\Coloneqq}},
        {->} = {\ensuremath{\to}},
        {:in:} = {\ensuremath{\in}},
      },
    or-sym = $|$,
    prod-sep = 2pt,
    row-sep = 0pt,
  }
